 <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>City Library | Management System</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body {
            font-family: 'Inter', sans-serif;
        }
        /* Custom scrollbar for better aesthetics */
        ::-webkit-scrollbar {
            width: 8px;
        }
        ::-webkit-scrollbar-track {
            background: #f1f1f1; 
        }
        ::-webkit-scrollbar-thumb {
            background: #888;
            border-radius: 4px;
        }
        ::-webkit-scrollbar-thumb:hover {
            background: #555;
        }
        .section {
            display: none;
        }
        .section.active {
            display: block;
        }
        .book-card {
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }
        .book-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
        }
        .modal-overlay {
            transition: opacity 0.3s ease;
        }
        .modal-content {
            transition: transform 0.3s ease;
        }
        .spinner {
            border: 4px solid rgba(0, 0, 0, 0.1);
            width: 24px;
            height: 24px;
            border-radius: 50%;
            border-left-color: #4f46e5;
            animation: spin 1s ease infinite;
        }
        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
    </style>
</head>
<body class="bg-gray-100 text-gray-800">

    <!-- Header -->
    <header class="bg-white shadow-md sticky top-0 z-40">
        <nav class="container mx-auto px-6 py-4 flex justify-between items-center">
            <a href="#" class="text-2xl font-bold text-indigo-600">
                <i class="fas fa-book-open-reader mr-2"></i>City Library
            </a>
            <div class="hidden md:flex space-x-6">
                <a href="#home" class="nav-link text-gray-600 hover:text-indigo-600">Home</a>
                <a href="#books" class="nav-link text-gray-600 hover:text-indigo-600">Browse Books</a>
                <a href="#add" class="nav-link text-gray-600 hover:text-indigo-600">Add Book</a>
                <a href="#contact" class="nav-link text-gray-600 hover:text-indigo-600">Contact</a>
            </div>
            <div class="md:hidden">
                <button id="mobile-menu-button" class="text-gray-600 hover:text-indigo-600 focus:outline-none">
                    <i class="fas fa-bars text-2xl"></i>
                </button>
            </div>
        </nav>
        <!-- Mobile Menu -->
        <div id="mobile-menu" class="hidden md:hidden bg-white">
            <a href="#home" class="nav-link block px-6 py-3 text-gray-600 hover:bg-indigo-50">Home</a>
            <a href="#books" class="nav-link block px-6 py-3 text-gray-600 hover:bg-indigo-50">Browse Books</a>
            <a href="#add" class="nav-link block px-6 py-3 text-gray-600 hover:bg-indigo-50">Add Book</a>
            <a href="#contact" class="nav-link block px-6 py-3 text-gray-600 hover:bg-indigo-50">Contact</a>
        </div>
    </header>

    <!-- Main Content -->
    <main class="container mx-auto px-6 py-8">
        <!-- Home Section -->
        <section id="home" class="section active">
            <div class="bg-white p-8 rounded-lg shadow-lg text-center">
                 <div class="w-full h-64 bg-cover bg-center rounded-lg mb-6" style="background-image: url('https://placehold.co/1200x400/667eea/ffffff?text=Welcome+to+City+Library');">
                </div>
                <h1 class="text-4xl font-bold text-gray-800 mb-4">Welcome to the City Library Management System</h1>
                <p class="text-lg text-gray-600 mb-6">Your digital gateway to a world of knowledge. Manage, discover, and borrow books with ease.</p>
                <a href="#books" class="nav-link bg-indigo-600 text-white font-semibold px-6 py-3 rounded-lg hover:bg-indigo-700 transition duration-300">Explore Our Collection</a>
            </div>
        </section>

        <!-- Books Section -->
        <section id="books" class="section">
            <div class="bg-white p-8 rounded-lg shadow-lg">
                <h2 class="text-3xl font-bold mb-6 text-gray-800">Book Collection</h2>
                <!-- Search and Filter -->
                <div class="flex flex-col md:flex-row justify-between items-center mb-6 space-y-4 md:space-y-0">
                    <div class="relative w-full md:w-1/3">
                        <input type="text" id="searchInput" class="w-full pl-10 pr-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500" placeholder="Search by title or author...">
                        <i class="fas fa-search absolute left-3 top-1/2 transform -translate-y-1/2 text-gray-400"></i>
                    </div>
                    <div class="flex items-center space-x-4">
                        <label for="filterSelect" class="font-medium">Filter by:</label>
                        <select id="filterSelect" class="border rounded-lg p-2 focus:outline-none focus:ring-2 focus:ring-indigo-500">
                            <option value="all">All</option>
                            <option value="available">Available</option>
                            <option value="issued">Issued</option>
                        </select>
                    </div>
                </div>

                <!-- Book List -->
                <div id="bookList" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                    <!-- Book cards will be injected here by JavaScript -->
                </div>
                 <div id="noResults" class="hidden text-center py-10">
                    <i class="fas fa-book text-4xl text-gray-300 mb-4"></i>
                    <p class="text-gray-500 text-lg">No books found matching your criteria.</p>
                </div>
            </div>
        </section>

        <!-- Add Book Section -->
        <section id="add" class="section">
            <div class="bg-white p-8 rounded-lg shadow-lg max-w-2xl mx-auto">
                <h2 class="text-3xl font-bold mb-6 text-gray-800">Add a New Book</h2>
                <form id="addBookForm">
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-4">
                        <div>
                            <label for="bookTitle" class="block text-gray-700 font-medium mb-2">Book Title</label>
                            <input type="text" id="bookTitle" class="w-full p-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500" required>
                        </div>
                        <div>
                            <label for="bookAuthor" class="block text-gray-700 font-medium mb-2">Author</label>
                            <input type="text" id="bookAuthor" class="w-full p-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500" required>
                        </div>
                    </div>
                     <div class="mb-4">
                        <button type="button" id="autofillBtn" class="w-full bg-purple-600 text-white font-semibold px-4 py-2 rounded-lg hover:bg-purple-700 transition duration-300 flex items-center justify-center">
                            <span id="autofillBtnText">✨ Auto-fill Details</span>
                            <div id="autofillSpinner" class="spinner hidden ml-2"></div>
                        </button>
                    </div>
                    <div class="mb-4">
                        <label for="bookGenre" class="block text-gray-700 font-medium mb-2">Genre</label>
                        <input type="text" id="bookGenre" class="w-full p-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500" required>
                    </div>
                    <div class="mb-6">
                         <label for="bookCover" class="block text-gray-700 font-medium mb-2">Cover Image URL (Optional)</label>
                         <input type="url" id="bookCover" class="w-full p-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500" placeholder="https://example.com/cover.jpg">
                    </div>
                    <div class="text-right">
                        <button type="submit" class="bg-indigo-600 text-white font-semibold px-6 py-3 rounded-lg hover:bg-indigo-700 transition duration-300">
                            <i class="fas fa-plus mr-2"></i>Add Book
                        </button>
                    </div>
                </form>
            </div>
        </section>

        <!-- Contact Section -->
        <section id="contact" class="section">
            <div class="bg-white p-8 rounded-lg shadow-lg">
                <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
                    <div>
                        <h2 class="text-3xl font-bold mb-4 text-gray-800">Contact Us</h2>
                        <p class="text-gray-600 mb-6">Have questions? We'd love to hear from you. Reach out to us through any of the methods below.</p>
                        <div class="space-y-4">
                            <p class="flex items-center text-gray-700"><i class="fas fa-map-marker-alt w-6 mr-2 text-indigo-600"></i>123 Library Lane, Knowledge City, 560001</p>
                            <p class="flex items-center text-gray-700"><i class="fas fa-phone w-6 mr-2 text-indigo-600"></i>+91 80 1234 5678</p>
                            <p class="flex items-center text-gray-700"><i class="fas fa-envelope w-6 mr-2 text-indigo-600"></i>info@citylibrary.com</p>
                        </div>
                    </div>
                     <div>
                        <h3 class="text-2xl font-bold mb-4 text-gray-800">Opening Hours</h3>
                        <ul class="text-gray-600 space-y-2">
                           <li><strong>Monday - Friday:</strong> 9:00 AM - 8:00 PM</li>
                           <li><strong>Saturday:</strong> 10:00 AM - 6:00 PM</li>
                           <li><strong>Sunday:</strong> Closed</li>
                        </ul>
                    </div>
                </div>
            </div>
        </section>
    </main>
    
    <!-- Book Details Modal -->
    <div id="bookModal" class="modal-overlay fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center p-4 z-50 hidden opacity-0">
        <div id="modalContent" class="modal-content bg-white rounded-lg shadow-xl max-w-2xl w-full max-h-[90vh] overflow-y-auto p-6 relative transform scale-95">
             <button id="closeModalBtn" class="absolute top-4 right-4 text-gray-500 hover:text-gray-800 text-2xl">&times;</button>
             <div id="modalBody">
                <!-- Content will be injected by JS -->
             </div>
        </div>
    </div>

    <!-- Footer -->
    <footer class="bg-white mt-8 py-6 shadow-inner">
        <div class="container mx-auto px-6 text-center text-gray-600">
            <p>&copy; <span id="currentYear"></span> City Library. All Rights Reserved.</p>
        </div>
    </footer>
    
    <!-- Custom Toast Notification -->
    <div id="toast" class="fixed bottom-5 right-5 bg-green-500 text-white py-2 px-4 rounded-lg shadow-lg opacity-0 translate-y-10 transition-all duration-300 z-50">
        <span id="toastMessage"></span>
    </div>


    <script>
        document.addEventListener('DOMContentLoaded', () => {

            // --- STATE MANAGEMENT ---
            let books = [
                { id: 1, title: 'The Great Gatsby', author: 'F. Scott Fitzgerald', genre: 'Classic', status: 'available', cover: 'https://placehold.co/300x450/9ca3af/ffffff?text=The+Great+Gatsby' },
                { id: 2, title: 'To Kill a Mockingbird', author: 'Harper Lee', genre: 'Classic', status: 'issued', cover: 'https://placehold.co/300x450/9ca3af/ffffff?text=To+Kill+a+Mockingbird' },
                { id: 3, title: '1984', author: 'George Orwell', genre: 'Dystopian', status: 'available', cover: 'https://placehold.co/300x450/9ca3af/ffffff?text=1984' },
                { id: 4, title: 'The Lord of the Rings', author: 'J.R.R. Tolkien', genre: 'Fantasy', status: 'available', cover: 'https://placehold.co/300x450/9ca3af/ffffff?text=The+Lord+of+the+Rings' },
                { id: 5, title: 'The Catcher in the Rye', author: 'J.D. Salinger', genre: 'Classic', status: 'issued', cover: 'https://placehold.co/300x450/9ca3af/ffffff?text=The+Catcher+in+the+Rye' },
                { id: 6, title: 'Pride and Prejudice', author: 'Jane Austen', genre: 'Romance', status: 'available', cover: 'https://placehold.co/300x450/9ca3af/ffffff?text=Pride+and+Prejudice' },
            ];

            // --- DOM ELEMENTS ---
            const bookList = document.getElementById('bookList');
            const searchInput = document.getElementById('searchInput');
            const filterSelect = document.getElementById('filterSelect');
            const addBookForm = document.getElementById('addBookForm');
            const sections = document.querySelectorAll('.section');
            const navLinks = document.querySelectorAll('.nav-link');
            const mobileMenuButton = document.getElementById('mobile-menu-button');
            const mobileMenu = document.getElementById('mobile-menu');
            const noResults = document.getElementById('noResults');
            const toast = document.getElementById('toast');
            const toastMessage = document.getElementById('toastMessage');
            const bookModal = document.getElementById('bookModal');
            const modalContent = document.getElementById('modalContent');
            const modalBody = document.getElementById('modalBody');
            const closeModalBtn = document.getElementById('closeModalBtn');
            const autofillBtn = document.getElementById('autofillBtn');

            // --- GEMINI API HELPER ---
            const callGeminiAPI = async (prompt, retries = 3, delay = 1000) => {
                const apiKey = ""; // Canvas will provide this
                const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-preview-05-20:generateContent?key=${apiKey}`;

                const payload = { contents: [{ parts: [{ text: prompt }] }] };

                for (let i = 0; i < retries; i++) {
                    try {
                        const response = await fetch(apiUrl, {
                            method: 'POST',
                            headers: { 'Content-Type': 'application/json' },
                            body: JSON.stringify(payload)
                        });

                        if (!response.ok) {
                            throw new Error(`HTTP error! status: ${response.status}`);
                        }

                        const result = await response.json();
                        const candidate = result.candidates?.[0];
                        if (candidate && candidate.content?.parts?.[0]?.text) {
                            return candidate.content.parts[0].text;
                        } else {
                            throw new Error("Invalid response structure from Gemini API");
                        }
                    } catch (error) {
                        console.error(`API call attempt ${i + 1} failed:`, error);
                        if (i === retries - 1) {
                            showToast('Failed to get response from AI. Please try again.', 'error');
                            return null;
                        }
                        await new Promise(res => setTimeout(res, delay * Math.pow(2, i))); // Exponential backoff
                    }
                }
            };

            // --- RENDER FUNCTION ---
            const renderBooks = () => {
                bookList.innerHTML = '';
                const searchTerm = searchInput.value.toLowerCase();
                const filterValue = filterSelect.value;

                const filteredBooks = books.filter(book => {
                    const matchesSearch = book.title.toLowerCase().includes(searchTerm) || book.author.toLowerCase().includes(searchTerm);
                    const matchesFilter = filterValue === 'all' || book.status === filterValue;
                    return matchesSearch && matchesFilter;
                });
                
                if (filteredBooks.length === 0) {
                    noResults.style.display = 'block';
                } else {
                    noResults.style.display = 'none';
                }

                filteredBooks.forEach(book => {
                    const isAvailable = book.status === 'available';
                    const card = document.createElement('div');
                    card.className = 'book-card bg-gray-50 border rounded-lg p-4 flex flex-col';
                    card.innerHTML = `
                        <img src="${book.cover}" alt="${book.title}" class="w-full h-64 object-cover rounded-md mb-4" onerror="this.onerror=null;this.src='https://placehold.co/300x450/e2e8f0/64748b?text=No+Image';">
                        <h3 class="text-xl font-bold text-gray-800 mb-2">${book.title}</h3>
                        <p class="text-gray-600 mb-1">by ${book.author}</p>
                        <p class="text-sm text-gray-500 mb-4">${book.genre}</p>
                        <div class="mt-auto pt-4 border-t border-gray-200 flex justify-between items-center">
                             <button class="details-btn text-indigo-600 hover:text-indigo-800 text-sm font-semibold py-2 px-4 rounded-lg transition duration-300" data-id="${book.id}">Details</button>
                             <span class="px-3 py-1 text-sm font-semibold rounded-full ${isAvailable ? 'bg-green-100 text-green-800' : 'bg-yellow-100 text-yellow-800'}">
                                ${isAvailable ? 'Available' : 'Issued'}
                            </span>
                        </div>
                    `;
                    bookList.appendChild(card);
                });
            };
            
            // --- MODAL FUNCTIONS ---
            const showModal = (book) => {
                 modalBody.innerHTML = `
                    <div class="flex flex-col md:flex-row gap-6">
                        <div class="md:w-1/3">
                            <img src="${book.cover}" alt="${book.title}" class="w-full h-auto object-cover rounded-lg shadow-md" onerror="this.onerror=null;this.src='https://placehold.co/300x450/e2e8f0/64748b?text=No+Image';">
                        </div>
                        <div class="md:w-2/3">
                            <h2 class="text-3xl font-bold text-gray-800 mb-2">${book.title}</h2>
                            <p class="text-gray-600 mb-2">by ${book.author}</p>
                            <p class="text-sm text-gray-500 mb-4">${book.genre}</p>
                            <button class="action-btn-modal ${book.status === 'available' ? 'bg-indigo-600 hover:bg-indigo-700' : 'bg-gray-600 hover:bg-gray-700'} text-white font-semibold px-4 py-2 rounded-lg transition duration-300 mb-6" data-id="${book.id}">
                                ${book.status === 'available' ? 'Issue Book' : 'Return Book'}
                            </button>
                            <div class="border-t pt-4">
                                <button id="aiSummaryBtn" class="w-full bg-teal-500 text-white font-semibold px-4 py-2 rounded-lg hover:bg-teal-600 transition duration-300 flex items-center justify-center">
                                    <span id="aiSummaryBtnText">✨ Get AI Summary & Suggestions</span>
                                    <div id="aiSummarySpinner" class="spinner hidden ml-2"></div>
                                </button>
                                <div id="aiSummaryContent" class="mt-4 text-gray-600 space-y-4 prose max-w-none"></div>
                            </div>
                        </div>
                    </div>
                 `;
                 bookModal.classList.remove('hidden');
                 setTimeout(() => {
                     bookModal.classList.remove('opacity-0');
                     modalContent.classList.remove('scale-95');
                 }, 10);
            };

            const hideModal = () => {
                bookModal.classList.add('opacity-0');
                modalContent.classList.add('scale-95');
                setTimeout(() => {
                    bookModal.classList.add('hidden');
                    modalBody.innerHTML = '';
                }, 300);
            };

            // --- NAVIGATION ---
            const showSection = (hash) => {
                sections.forEach(section => {
                    if (`#${section.id}` === hash) {
                        section.classList.add('active');
                    } else {
                        section.classList.remove('active');
                    }
                });
                 mobileMenu.classList.add('hidden'); // Hide mobile menu on navigation
            };

            navLinks.forEach(link => {
                link.addEventListener('click', (e) => {
                    e.preventDefault();
                    const hash = new URL(link.href).hash;
                    window.location.hash = hash;
                });
            });

            window.addEventListener('hashchange', () => {
                 const hash = window.location.hash || '#home';
                 showSection(hash);
            });
            
            // Initial page load
            showSection(window.location.hash || '#home');
            
            // --- EVENT LISTENERS ---
            searchInput.addEventListener('input', renderBooks);
            filterSelect.addEventListener('change', renderBooks);

            // Event delegation for various buttons
            document.body.addEventListener('click', async (e) => {
                // Book details modal button
                if (e.target.classList.contains('details-btn')) {
                    const bookId = parseInt(e.target.dataset.id);
                    const book = books.find(b => b.id === bookId);
                    if (book) showModal(book);
                }
                
                // Issue/Return button inside modal
                if (e.target.classList.contains('action-btn-modal')) {
                    const bookId = parseInt(e.target.dataset.id);
                    const book = books.find(b => b.id === bookId);
                    if (book) {
                        book.status = book.status === 'available' ? 'issued' : 'available';
                        const message = `"${book.title}" has been ${book.status === 'issued' ? 'issued' : 'returned'}.`;
                        showToast(message, 'success');
                        hideModal();
                        renderBooks();
                    }
                }
                
                // AI Summary button
                if (e.target.id === 'aiSummaryBtn' || e.target.parentElement.id === 'aiSummaryBtn') {
                    const btn = document.getElementById('aiSummaryBtn');
                    const btnText = document.getElementById('aiSummaryBtnText');
                    const spinner = document.getElementById('aiSummarySpinner');
                    const contentDiv = document.getElementById('aiSummaryContent');
                    
                    const bookTitle = modalBody.querySelector('h2').textContent;
                    const bookAuthor = modalBody.querySelector('p').textContent.replace('by ', '');
                    
                    btn.disabled = true;
                    btnText.textContent = 'Generating...';
                    spinner.classList.remove('hidden');
                    
                    const prompt = `Provide a concise, one-paragraph summary for the book "${bookTitle}" by ${bookAuthor}. After the summary, add a heading "Read-Alike Suggestions" and list three similar books with their authors. Format the suggestions as a bulleted list.`;
                    const result = await callGeminiAPI(prompt);
                    
                    if (result) {
                        contentDiv.innerHTML = result.replace(/\n\n/g, '<br>').replace(/\n/g, '<br>').replace(/\* /g, '<br>&bull; ');
                    } else {
                        contentDiv.innerHTML = `<p class="text-red-500">Could not generate summary. Please try again later.</p>`;
                    }
                    
                    btn.disabled = false;
                    spinner.classList.add('hidden');
                    btnText.textContent = '✨ Get AI Summary & Suggestions';
                }
            });


            addBookForm.addEventListener('submit', (e) => {
                e.preventDefault();
                const title = document.getElementById('bookTitle').value;
                const author = document.getElementById('bookAuthor').value;
                const genre = document.getElementById('bookGenre').value;
                const cover = document.getElementById('bookCover').value;
                
                const newBook = {
                    id: books.length > 0 ? Math.max(...books.map(b => b.id)) + 1 : 1,
                    title,
                    author,
                    genre,
                    status: 'available',
                    cover: cover || `https://placehold.co/300x450/9ca3af/ffffff?text=${encodeURIComponent(title)}`
                };

                books.unshift(newBook); // Add to the beginning of the array
                addBookForm.reset();
                showToast(`"${title}" was successfully added!`, 'success');
                window.location.hash = '#books';
                renderBooks();
            });

            // Autofill Genre button
            autofillBtn.addEventListener('click', async () => {
                const title = document.getElementById('bookTitle').value;
                const author = document.getElementById('bookAuthor').value;
                
                if (!title || !author) {
                    showToast('Please enter a title and author first.', 'error');
                    return;
                }

                const btnText = document.getElementById('autofillBtnText');
                const spinner = document.getElementById('autofillSpinner');
                
                autofillBtn.disabled = true;
                btnText.classList.add('hidden');
                spinner.classList.remove('hidden');
                
                const prompt = `Based on the book title "${title}" by "${author}", what is its primary genre? Respond with only the genre name.`;
                const result = await callGeminiAPI(prompt);
                
                if(result) {
                    document.getElementById('bookGenre').value = result.trim();
                    showToast('Genre auto-filled!', 'success');
                }
                
                autofillBtn.disabled = false;
                btnText.classList.remove('hidden');
                spinner.classList.add('hidden');
            });

            mobileMenuButton.addEventListener('click', () => {
                mobileMenu.classList.toggle('hidden');
            });

            closeModalBtn.addEventListener('click', hideModal);
            bookModal.addEventListener('click', (e) => {
                if (e.target === bookModal) hideModal();
            });
            
            // --- UTILITY FUNCTIONS ---
            
            // Toast Notification
            function showToast(message, type = 'success') {
                toastMessage.textContent = message;
                toast.className = `fixed bottom-5 right-5 text-white py-2 px-4 rounded-lg shadow-lg transition-all duration-300 z-50 ${type === 'success' ? 'bg-green-500' : 'bg-red-500'}`;
                toast.classList.remove('opacity-0', 'translate-y-10');
                
                setTimeout(() => {
                    toast.classList.add('opacity-0', 'translate-y-10');
                }, 3000);
            }

            // Set current year in footer
            document.getElementById('currentYear').textContent = new Date().getFullYear();

            // --- INITIAL RENDER ---
            renderBooks();
        });
    </script>
</body>
</html>
